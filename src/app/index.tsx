import React from "react";
import { StyleSheet, Text, View } from "react-native";

const NotesComponent = () => {
  return (
    <View>
      <Text>NotesComponent</Text>
    </View>
  );
};

export default NotesComponent;

const styles = StyleSheet.create({});
